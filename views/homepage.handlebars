{{#each posts as |post|}}
<a class="list-group btn list-item main-list single-post" href="/post/{{post.id}}">
    <span class="float-left">{{post.title}}</span>
    <span class="float-right">Posted by:{{user.id}} on {{format_date date_created}}</span>
</a>
{{/each}}